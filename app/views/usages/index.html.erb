<%= high_chart("my_id", @h) do |c| %>
	<%= "options.tooltip.formatter = function() {return Highcharts.dateFormat('%B %e, %Y - %H:%M:%S', this.x) + ': ' + Highcharts.numberFormat(this.y, 2) + ' kwh';
  }" %>
<% end%>